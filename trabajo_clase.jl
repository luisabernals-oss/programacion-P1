# 1.Calcular perımetro y area de cuadrado dado EL LADO L
println("Respetado ususario, vamos a calcular perímetro y área de un cuadrado")
println("Digite ahora el valor del lado")
l=parse(Float64,readline())
Perímetro=4*l
Area=l*l
println("El perímetro es $Perímetro y el area es $Area")

# Determinar el volumen de un cilindro utilizando su radio r y altura h. Formula: V = πr2h.
println("Vamos a calcular el volumen de un cilindro")
=parse(Float64,readline())
pi=3.1427
r=parse(Float64,readline())
h=parse(Float64,readline())
Volumen=pi*r^2*h



